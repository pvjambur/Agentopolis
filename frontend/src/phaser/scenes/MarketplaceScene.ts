/** Main isometric marketplace scene — loads tilemap, spawns agents, handles WebSocket events.
 * Implemented Phase 1 (world) → Phase 3 (agents + negotiation) → Phase 4 (payment events).
 */
export class MarketplaceScene {
  // Stub — implemented Phase 1
}
