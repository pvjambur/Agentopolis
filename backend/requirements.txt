# Agentopolis backend — direct dependencies only
# Transitive deps are resolved by pip at install time

# Web framework
fastapi==0.115.14
uvicorn[standard]==0.30.6
python-multipart==0.0.12
starlette==0.46.2

# Config & validation
pydantic==2.13.4
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Database
supabase==2.16.0
asyncpg==0.29.0
alembic==1.13.3
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9

# Vector DB
pinecone-client==6.0.0

# LLM
anthropic==0.116.0
groq==0.37.1

# Embeddings
fastembed==0.8.0

# Task queue
celery==5.6.3
redis==6.4.0

# Auth
python-jose[cryptography]==3.3.0
PyJWT==2.13.0
cryptography==49.0.0

# Payments
razorpay==2.0.1

# Observability
langfuse==2.60.10
sentry-sdk==2.66.1

# HTTP & utils
httpx==0.28.1
orjson==3.11.9
tenacity==9.1.4
anyio==4.13.0
aiofiles==24.1.0

# Rate limiting
slowapi==0.1.9

# Dev tools (also needed in prod for ruff checks)
ruff==0.16.1
pytest==9.1.0
pytest-asyncio==1.4.0
pytest-cov==7.1.0
